name: pr
on: pull_request
jobs:
  pr-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: hi
        run: |
          echo "Hello World on pull_request!"
          cat hello.txt
